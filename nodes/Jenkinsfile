pipeline {
    agent {
        label 'master'
    }
    environment {
        test = "None"
        AWS_ACCESS_KEY_ID = credentials('aws_access_key_id')
        AWS_SECRET_ACCESS_KEY = credentials('aws_secret_access_key')
    }
    stages {
        stage('Fetch Infrastructure Code') {
            steps {
                git(
                    url: 'https://github.com/Chambras/ChefConf2019.git',
                    credentialsId: 'chef-conf-github',
                    branch: 'dev'
                )
                sh 'ls -la'
            }
        }
        stage('Initialize Node Folders') {
            steps {
                echo "Running init to check conectivity to remote state"
                sh '''#!/bin/bash
                cd Terraform/AWS/TestNodes
                terraform init
                '''
                withCredentials([azureServicePrincipal('azure-sp-demo')]) {
                    sh '''#!/bin/bash
                    cd Terraform/Azure/TestNodes
                    terraform init
                    '''
                }
                
            }
        }
    }
}